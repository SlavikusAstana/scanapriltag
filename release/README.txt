Windows x64 build archive: AprilTagScanner-win-x64.zip

Create locally:
  build.bat
  powershell Compress-Archive -Path dist\AprilTagScanner -DestinationPath release\AprilTagScanner-win-x64.zip

The zip is published on GitHub Releases (too large for regular git commits).
